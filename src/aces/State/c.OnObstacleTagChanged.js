export const config = {
  listName: "On obstacle tag changed",
  displayText: "On obstacle tag changed",
  description: "Triggered after the primary obstacle tag changes in tag mode.",
  isTrigger: true,
  params: [],
};

export const expose = true;

export default function () {
  return true;
}