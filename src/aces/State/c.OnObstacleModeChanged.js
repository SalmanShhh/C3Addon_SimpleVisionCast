export const config = {
  listName: "On obstacle mode changed",
  displayText: "On obstacle mode changed",
  description: "Triggered after the active obstacle mode changes.",
  isTrigger: true,
  params: [],
};

export const expose = true;

export default function () {
  return true;
}